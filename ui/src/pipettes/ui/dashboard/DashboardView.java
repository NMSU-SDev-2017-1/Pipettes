package pipettes.ui.dashboard;

import com.airhacks.afterburner.views.FXMLView;

public class DashboardView extends FXMLView
{
  // FXML and CSS are automatically loaded and instantiated.
  // Nothing to do unless View normally does nothing unless a custom
  // constructor is needed.
}
